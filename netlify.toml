[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — toutes les routes renvoient vers index.html
# (utile dès que tu ajouteras du routing côté client)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
