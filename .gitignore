node_modules/
dist/
*.local
.DS_Store

# Secrets / variables d'environnement — ne JAMAIS committer (clés API).
.env
.env.*
!.env.example
node_modules/.tmp/
mira2react.tar.gz
.gstack/

# docs/ : on VERSIONNE la documentation technique (.md), mais PAS les binaires business
# confidentiels (pitch investisseurs, cap table, kits, notes). Ne JAMAIS committer ces
# binaires — ils restent en local uniquement.
docs/*.pdf
docs/*.pptx
docs/*.ppt
docs/*.pages
docs/*.docx
docs/*.doc
docs/*.key
docs/article
examples of reports/
# Local Netlify folder
.netlify
