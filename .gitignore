node_modules/
dist/
*.local
.DS_Store

# Secrets / variables d'environnement — ne JAMAIS committer (clés API).
.env
.env.*
!.env.example
node_modules/.tmp/
mira2react.tar.gz
.gstack/

# Documents business confidentiels (récupérés de Notion : cap table, valorisation,
# pitch investisseurs). Ne JAMAIS committer — restent en local uniquement.
docs/
examples of reports/
# Local Netlify folder
.netlify
